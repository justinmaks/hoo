name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.goos }}/${{ matrix.goarch }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            goos: linux
            goarch: amd64
            cc: gcc

          - runner: ubuntu-latest
            goos: linux
            goarch: arm64
            cc: aarch64-linux-gnu-gcc
            install_cross: true

          - runner: macos-latest
            goos: darwin
            goarch: amd64
            cc: clang
            cgo_cflags: "-arch x86_64"
            cgo_ldflags: "-arch x86_64"

          - runner: macos-latest
            goos: darwin
            goarch: arm64
            cc: clang

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # needed for git describe --tags

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Install libpcap (Linux native)
        if: runner.os == 'Linux' && !matrix.install_cross
        run: sudo apt-get install -y libpcap-dev

      - name: Install libpcap + cross-compiler (Linux arm64)
        if: matrix.install_cross
        run: |
          sudo dpkg --add-architecture arm64

          # Ubuntu 24.04 uses DEB822 format (.sources files); older releases
          # use the one-liner sources.list format. Restrict the existing entries
          # to amd64 only — arm64 packages live on ports.ubuntu.com, not on
          # archive.ubuntu.com or security.ubuntu.com.
          if [ -f /etc/apt/sources.list.d/ubuntu.sources ]; then
            sudo sed -i '/^Types: deb$/a Architectures: amd64' \
              /etc/apt/sources.list.d/ubuntu.sources
          else
            sudo sed -i 's/^deb /deb [arch=amd64] /' /etc/apt/sources.list
          fi

          # Add arm64 sources from ports.ubuntu.com
          CODENAME=$(lsb_release -cs)
          {
            echo "deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports ${CODENAME} main restricted universe multiverse"
            echo "deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports ${CODENAME}-updates main restricted universe multiverse"
            echo "deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports ${CODENAME}-security main restricted universe multiverse"
          } | sudo tee /etc/apt/sources.list.d/arm64-ports.list

          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu libpcap-dev:arm64

      - name: Set build metadata
        id: meta
        shell: bash
        run: |
          VERSION=${GITHUB_REF_NAME}
          COMMIT=$(git rev-parse --short HEAD)
          DATE=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          BINARY=hoo-${VERSION}-${{ matrix.goos }}-${{ matrix.goarch }}
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "commit=${COMMIT}"   >> "$GITHUB_OUTPUT"
          echo "date=${DATE}"       >> "$GITHUB_OUTPUT"
          echo "binary=${BINARY}"   >> "$GITHUB_OUTPUT"

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "1"
          CC: ${{ matrix.cc }}
          # For arm64 cross-compilation, point directly at the arm64 libpcap
          # instead of relying on pkg-config finding the right multiarch path.
          CGO_CFLAGS:  ${{ matrix.install_cross && '-I/usr/include' || matrix.cgo_cflags || '' }}
          CGO_LDFLAGS: ${{ matrix.install_cross && '-L/usr/lib/aarch64-linux-gnu -lpcap' || matrix.cgo_ldflags || '' }}
        run: |
          go build \
            -trimpath \
            -ldflags "-s -w \
              -X 'github.com/justinmaks/hoo/internal/cmd.version=${{ steps.meta.outputs.version }}' \
              -X 'github.com/justinmaks/hoo/internal/cmd.commit=${{ steps.meta.outputs.commit }}' \
              -X 'github.com/justinmaks/hoo/internal/cmd.date=${{ steps.meta.outputs.date }}'" \
            -o "${{ steps.meta.outputs.binary }}" \
            ./cmd/hoo

      - name: Create archive
        shell: bash
        run: |
          BINARY=${{ steps.meta.outputs.binary }}
          if [ "${{ matrix.goos }}" = "windows" ]; then
            zip "${BINARY}.zip" "${BINARY}.exe" README.md LICENSE
            echo "ASSET=${BINARY}.zip" >> "$GITHUB_ENV"
          else
            tar czf "${BINARY}.tar.gz" "${BINARY}" README.md LICENSE
            echo "ASSET=${BINARY}.tar.gz" >> "$GITHUB_ENV"
          fi

      - name: Generate checksum
        run: sha256sum "$ASSET" > "$ASSET.sha256"

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ steps.meta.outputs.binary }}
          path: |
            ${{ env.ASSET }}
            ${{ env.ASSET }}.sha256
          retention-days: 1

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Generate release notes
        id: notes
        run: |
          VERSION=${GITHUB_REF_NAME}
          PREV=$(git describe --tags --abbrev=0 "${VERSION}^" 2>/dev/null || echo "")

          {
            echo "## What's Changed"
            echo ""
            if [ -n "$PREV" ]; then
              git log --pretty=format:"- %s (%h)" "${PREV}..${VERSION}"
            else
              git log --pretty=format:"- %s (%h)"
            fi
            echo ""
            echo ""
            echo "## Installation"
            echo ""
            echo "Download the archive for your platform, extract, and move \`hoo\` to your PATH."
            echo ""
            echo "**Linux** — requires \`libpcap\`:"
            echo "\`\`\`bash"
            echo "sudo apt install libpcap-dev   # Debian/Ubuntu"
            echo "sudo dnf install libpcap-devel # Fedora/RHEL"
            echo "\`\`\`"
            echo ""
            echo "**Privileges:** see the README for \`setcap\` or \`sudo\` instructions."
            echo ""
            echo "## Checksums"
            echo ""
            echo "\`\`\`"
            cat dist/*.sha256
            echo "\`\`\`"
          } > release_notes.md

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          body_path: release_notes.md
          files: dist/**
          fail_on_unmatched_files: true
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
